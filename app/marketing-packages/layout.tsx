import { staticMetadata } from "../lib/seo/service";
export const metadata = staticMetadata("Seattle Desi TV Sponsorship and Marketing", "Explore sponsorship, community media, promotion, and partnership opportunities with Seattle Desi TV.", "/marketing-packages");
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
