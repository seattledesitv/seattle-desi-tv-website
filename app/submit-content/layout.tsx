import { staticMetadata } from "../lib/seo/service";
export const metadata = staticMetadata("Submit Community Content", "Send Seattle Desi TV a community story, event, interview idea, announcement, or coverage request.", "/submit-content");
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
