import { staticMetadata } from "../lib/seo/service";
export const metadata = staticMetadata("Seattle Community Classifieds", "Browse approved community classifieds, local items, services, opportunities, and announcements across the Seattle area.", "/classifieds");
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
