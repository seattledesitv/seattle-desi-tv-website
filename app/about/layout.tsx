import { staticMetadata } from "../lib/seo/service";
export const metadata = staticMetadata("About Our 501(c)(3) Community Organization", "Learn about Seattle Desi TV's nonprofit mission, vision, community media programs, and work celebrating South Asian culture across the Pacific Northwest.", "/about");
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
