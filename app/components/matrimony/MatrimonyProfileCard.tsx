import SafeImage from "../SafeImage";
import type { MatrimonyProfile } from "../../lib/matrimony/types";
export default function MatrimonyProfileCard({profile}:{profile:MatrimonyProfile}){
 const age=new Date().getFullYear()-profile.birth_year;
 return <article className="overflow-hidden rounded-3xl border bg-white shadow-sm"><div className="aspect-[4/3] bg-pink-50"><SafeImage src={profile.photo_urls?.[0]||""} alt={profile.display_name} className="h-full w-full object-cover" fallbackClassName="grid h-full w-full place-items-center bg-gradient-to-br from-pink-50 to-amber-50 text-2xl font-black text-pink-600" fallbackLabel="Private Profile" enableFullPreview={false}/></div><div className="p-6"><p className="text-xs font-black uppercase tracking-wide text-pink-600">Verified by SDTV</p><h3 className="mt-2 text-2xl font-black">{profile.display_name}</h3><p className="mt-1 text-slate-600">{age} · {profile.city}{profile.state_region?`, ${profile.state_region}`:""}</p><div className="mt-4 grid gap-2 text-sm"><p><b>Education:</b> {profile.education||"Not specified"}</p><p><b>Occupation:</b> {profile.occupation||"Not specified"}</p><p><b>Languages:</b> {profile.languages.join(", ")||"Not specified"}</p><p><b>Community:</b> {profile.community||"Open"}</p></div><p className="mt-4 text-sm leading-6 text-slate-600">{profile.about}</p><div className="mt-4 rounded-2xl bg-slate-50 p-4"><p className="font-black">Partner preferences</p><p className="mt-1 text-sm text-slate-600">{profile.partner_preferences}</p></div><p className="mt-4 text-xs text-slate-500">For privacy, SDTV facilitates introductions. Personal contact details are not displayed.</p></div></article>;
}
