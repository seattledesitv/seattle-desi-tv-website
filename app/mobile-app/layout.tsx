import { staticMetadata } from "../lib/seo/service";
export const metadata = staticMetadata("Seattle Desi TV Mobile App", "Learn about the Seattle Desi TV mobile experience for community events, media, radio, organizations, and local resources.", "/mobile-app");
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
