import { staticMetadata } from "../lib/seo/service";
export const metadata = staticMetadata("Seattle Desi Business Offers", "Explore current offers and promotions from approved South Asian businesses and community partners in the Seattle area.", "/offers");
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
