import { staticMetadata } from "../lib/seo/service";
export const metadata = staticMetadata("Seattle South Asian Creators and Influencers", "Meet South Asian creators, community voices, hosts, and influencers connected through the Seattle Desi TV network.", "/influencers");
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
