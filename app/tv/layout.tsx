import { staticMetadata } from "../lib/seo/service";
export const metadata = staticMetadata("Seattle Desi TV Community Video", "Watch Seattle Desi TV community interviews, cultural programs, event coverage, local stories, and video highlights.", "/tv");
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
