import { staticMetadata } from "../lib/seo/service";
export const metadata = staticMetadata("Share Your Community Story with SDTV", "Submit community stories, announcements, ideas, and media for consideration by Seattle Desi TV.", "/share-with-sdtv");
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
