import { staticMetadata } from "../lib/seo/service";
export const metadata = staticMetadata("Seattle South Asian Community Groups", "Discover cultural, regional, professional, social, and service-oriented South Asian community groups in Seattle and the Pacific Northwest.", "/community-groups");
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
