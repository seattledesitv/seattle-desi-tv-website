import { staticMetadata } from "../lib/seo/service";
export const metadata = staticMetadata("Seattle South Asian Community Organizations", "Find nonprofit, cultural, religious, professional, and service organizations supporting Seattle's South Asian community.", "/community-organizations");
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
