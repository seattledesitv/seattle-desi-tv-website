"use client";

import { useEffect, useMemo, useState } from "react";
import MyHubHeader from "../components/MyHubHeader";
import SiteFooter from "../components/SiteFooter";
import { getSupabaseBrowserClient } from "../lib/supabaseBrowser";

const supabase=getSupabaseBrowserClient();
function statusText(value?:string|null){const text=String(value||"pending").replaceAll("_"," ");return text.charAt(0).toUpperCase()+text.slice(1);}
function Field({label,children}:{label:string;children:any}){return <label className="grid gap-1 text-sm font-black text-slate-800"><span>{label}</span>{children}</label>}

type BusinessRow={id:string;name:string;address?:string;website?:string;category?:string;discount?:string;offer?:string;poc_name?:string;poc_email?:string;poc_phone?:string;image?:string;image_urls?:string[];status?:string;created_by?:string;owner_verified_at?:string;access_type?:"submitted"|"claimed";manager_role?:string};

export default function MyBusinessesPage(){
  const [rows,setRows]=useState<BusinessRow[]>([]),[message,setMessage]=useState("Loading..."),[saving,setSaving]=useState(false),[selectedId,setSelectedId]=useState(""),[editingId,setEditingId]=useState(""),[editForm,setEditForm]=useState<any>({}),[searchText,setSearchText]=useState(""),[statusFilter,setStatusFilter]=useState("all"),[user,setUser]=useState<any>(null);

  async function loadRows(){
    const {data:auth}=await supabase.auth.getUser();const currentUser=auth?.user||null;setUser(currentUser);
    if(!currentUser?.id){setMessage("Please login to view your submitted and claimed listings.");return;}
    const submittedResult=await supabase.from("local_businesses").select("id,name,address,website,category,discount,offer,poc_name,poc_email,poc_phone,image,image_urls,status,created_at,created_by,owner_verified_at").eq("created_by",currentUser.id).order("created_at",{ascending:false});
    const managedResult=await supabase.from("business_managers").select("role,is_primary,local_businesses(id,name,address,website,category,discount,offer,poc_name,poc_email,poc_phone,image,image_urls,status,created_at,created_by,owner_verified_at)").eq("user_id",currentUser.id).eq("active",true);
    if(submittedResult.error){setMessage(submittedResult.error.message);return;}
    const map=new Map<string,BusinessRow>();
    (submittedResult.data||[]).forEach((row:any)=>map.set(row.id,{...row,access_type:"submitted"}));
    if(!managedResult.error)(managedResult.data||[]).forEach((manager:any)=>{const business=manager.local_businesses;if(business)map.set(business.id,{...business,access_type:"claimed",manager_role:manager.role});});
    const nextRows=Array.from(map.values());setRows(nextRows);setSelectedId(current=>current&&nextRows.some(row=>row.id===current)?current:nextRows[0]?.id||"");
    setMessage(managedResult.error?`Submitted listings loaded, but claimed-business access could not be loaded: ${managedResult.error.message}`:"Submitted and claimed business profiles.");
  }
  useEffect(()=>{loadRows();},[]);
  const filteredRows=useMemo(()=>{const q=searchText.trim().toLowerCase();return rows.filter(row=>(statusFilter==="all"||String(row.status||"pending").toLowerCase()===statusFilter)&&(!q||[row.name,row.address,row.website,row.category,row.offer,row.poc_name,row.poc_email,row.poc_phone,row.status,row.access_type].some(value=>String(value||"").toLowerCase().includes(q))))},[rows,searchText,statusFilter]);
  const selectedRow=rows.find(row=>row.id===selectedId)||filteredRows[0]||null;
  function startEdit(row:BusinessRow){setSelectedId(row.id);setEditingId(row.id);setEditForm({name:row.name||"",address:row.address||"",website:row.website||"",category:row.category||"",discount:row.discount||"",offer:row.offer||"",poc_name:row.poc_name||"",poc_email:row.poc_email||"",poc_phone:row.poc_phone||""});}

  async function saveBusiness(row:BusinessRow){
    if(!user?.id)return;setSaving(true);setMessage("");
    const payload={name:editForm.name?.trim(),address:editForm.address?.trim(),website:editForm.website?.trim()||null,category:editForm.category?.trim()||null,discount:editForm.discount?.trim()||null,offer:editForm.offer?.trim()||null,poc_name:editForm.poc_name?.trim()||null,poc_email:editForm.poc_email?.trim()||null,poc_phone:editForm.poc_phone?.trim()||null};
    if(!payload.name||!payload.address){setSaving(false);setMessage("Business name and address are required.");return;}
    if(row.access_type==="claimed"&&row.created_by!==user.id){
      const suggestion=["Verified owner profile update:",...Object.entries(payload).map(([key,value])=>`${key}: ${value||"(remove)"}`)].join("\n");
      const result=await supabase.from("business_edit_suggestions").insert({business_id:row.id,submitter_user_id:user.id,submitter_name:user.user_metadata?.full_name||user.user_metadata?.name||user.email,submitter_email:user.email,suggestion,status:"pending"});
      setSaving(false);if(result.error){setMessage(result.error.message);return;}setEditingId("");setMessage("Your proposed changes were submitted to SDTV for approval.");return;
    }
    const result=await supabase.from("local_businesses").update({...payload,updated_at:new Date().toISOString()}).eq("id",row.id).eq("created_by",user.id);
    setSaving(false);if(result.error){setMessage(result.error.message);return;}setEditingId("");setMessage("Business listing updated.");await loadRows();
  }

  return <main className="min-h-screen bg-slate-950 text-white"><MyHubHeader/><section className="mx-auto max-w-7xl px-6 py-10"><div className="mb-8 flex flex-col gap-4 md:flex-row md:items-end md:justify-between"><div><p className="font-black uppercase tracking-wide text-pink-300">My Hub</p><h1 className="mt-2 text-4xl font-black md:text-5xl">My Businesses</h1><p className="mt-2 text-slate-300">{message}</p></div><a href="/businesses" className="rounded-xl bg-pink-600 px-5 py-3 text-center font-black text-white">Business Directory</a></div><section className="mb-6 rounded-3xl bg-white/10 p-4"><div className="grid gap-3 md:grid-cols-[1fr_180px]"><input value={searchText} onChange={e=>setSearchText(e.target.value)} placeholder="Search your businesses..." className="rounded-xl bg-white px-4 py-3 font-bold text-slate-950"/><select value={statusFilter} onChange={e=>setStatusFilter(e.target.value)} className="rounded-xl bg-white px-4 py-3 font-bold text-slate-950"><option value="all">All statuses</option><option value="pending">Pending</option><option value="approved">Approved</option><option value="rejected">Rejected</option></select></div></section>{rows.length===0?<div className="rounded-3xl bg-white p-8 text-slate-950">No submitted or claimed businesses found.</div>:<div className="grid gap-6 xl:grid-cols-[420px_1fr]"><aside className="grid h-fit max-h-[78vh] gap-4 overflow-y-auto">{filteredRows.map(row=><button key={row.id} onClick={()=>{setSelectedId(row.id);setEditingId("")}} className={`rounded-3xl border p-4 text-left text-slate-950 ${selectedRow?.id===row.id?"border-pink-500 bg-pink-50":"bg-white"}`}><div className="flex items-start justify-between gap-3"><h2 className="text-lg font-black">{row.name}</h2><span className="rounded-full bg-pink-50 px-2 py-1 text-[11px] font-black text-pink-600">{statusText(row.status)}</span></div><p className="mt-2 text-sm text-slate-600">{row.category||"Business"} · {row.address||"No address"}</p><p className="mt-2 text-xs font-black uppercase text-emerald-700">{row.access_type==="claimed"?`Claimed · ${row.manager_role||"owner"}`:"Submitted by you"}</p></button>)}</aside><section className="min-h-[560px] rounded-[2rem] bg-white p-6 text-slate-950 shadow-2xl">{!selectedRow?<p>Select a business.</p>:editingId===selectedRow.id?<div className="grid gap-4"><div className="flex justify-between gap-3"><div><p className="text-xs font-black uppercase text-pink-600">Editing profile</p><h2 className="text-3xl font-black">{selectedRow.name}</h2>{selectedRow.access_type==="claimed"&&selectedRow.created_by!==user?.id&&<p className="mt-2 text-sm text-amber-700">Changes to claimed profiles are sent to SDTV for approval before publishing.</p>}</div><button onClick={()=>setEditingId("")} className="rounded-xl bg-slate-100 px-4 py-3 font-black">Cancel</button></div><div className="grid gap-4 md:grid-cols-2"><Field label="Business name"><input className="rounded-xl border p-3 font-normal" value={editForm.name} onChange={e=>setEditForm({...editForm,name:e.target.value})}/></Field><Field label="Category"><input className="rounded-xl border p-3 font-normal" value={editForm.category} onChange={e=>setEditForm({...editForm,category:e.target.value})}/></Field><Field label="Address"><input className="rounded-xl border p-3 font-normal" value={editForm.address} onChange={e=>setEditForm({...editForm,address:e.target.value})}/></Field><Field label="Website"><input className="rounded-xl border p-3 font-normal" value={editForm.website} onChange={e=>setEditForm({...editForm,website:e.target.value})}/></Field><Field label="Discount / special offer"><input className="rounded-xl border p-3 font-normal" value={editForm.discount} onChange={e=>setEditForm({...editForm,discount:e.target.value})}/></Field><Field label="Contact name"><input className="rounded-xl border p-3 font-normal" value={editForm.poc_name} onChange={e=>setEditForm({...editForm,poc_name:e.target.value})}/></Field><Field label="Contact email"><input className="rounded-xl border p-3 font-normal" value={editForm.poc_email} onChange={e=>setEditForm({...editForm,poc_email:e.target.value})}/></Field><Field label="Contact phone"><input className="rounded-xl border p-3 font-normal" value={editForm.poc_phone} onChange={e=>setEditForm({...editForm,poc_phone:e.target.value})}/></Field><Field label="Description / offer"><textarea className="min-h-28 rounded-xl border p-3 font-normal" value={editForm.offer} onChange={e=>setEditForm({...editForm,offer:e.target.value})}/></Field></div><button onClick={()=>saveBusiness(selectedRow)} disabled={saving} className="rounded-xl bg-pink-600 px-5 py-4 font-black text-white disabled:opacity-60">{saving?"Saving...":selectedRow.access_type==="claimed"&&selectedRow.created_by!==user?.id?"Submit changes for approval":"Save Business Listing"}</button></div>:<div><div className="flex flex-wrap items-start justify-between gap-4"><div><div className="flex flex-wrap items-center gap-2"><h2 className="text-3xl font-black">{selectedRow.name}</h2>{selectedRow.owner_verified_at&&<span className="rounded-full bg-emerald-50 px-3 py-1 text-xs font-black text-emerald-700">✓ Owner verified</span>}</div><p className="mt-2 text-slate-600">{selectedRow.category||"Business"} · {selectedRow.address||"No address"}</p></div><button onClick={()=>startEdit(selectedRow)} className="rounded-xl bg-pink-600 px-5 py-3 font-black text-white">Edit profile</button></div><div className="mt-6 grid gap-4 rounded-2xl bg-slate-50 p-5 text-sm md:grid-cols-2"><p><b>Access:</b><br/>{selectedRow.access_type==="claimed"?`Claimed ${selectedRow.manager_role||"owner"}`:"Submitted by you"}</p><p><b>Status:</b><br/>{statusText(selectedRow.status)}</p><p><b>Website:</b><br/>{selectedRow.website||"—"}</p><p><b>Contact:</b><br/>{[selectedRow.poc_name,selectedRow.poc_email,selectedRow.poc_phone].filter(Boolean).join(" · ")||"—"}</p><p className="md:col-span-2"><b>Description / offer:</b><br/>{selectedRow.offer||"—"}</p></div></div>}</section></div>}</section><SiteFooter/></main>;
}
