"use client";

import { useState } from "react";
import MyHubHeader from "../../components/MyHubHeader";
import SiteFooter from "../../components/SiteFooter";
import { useBusinessOffers } from "../../hooks/useBusinessOffers";

const today = () => new Date().toISOString().slice(0, 10);
export default function MyBusinessOffersPage() { const { offers, businesses, loading, saving, error, create, remove } = useBusinessOffers("owner"); const [form, setForm] = useState({ business_id: "", title: "", description: "", terms: "", offer_code: "", destination_url: "", image_url: "", starts_at: today(), ends_at: "", status: "pending" as const }); const [message, setMessage] = useState(""); async function submit() { setMessage(""); try { await create(form); setForm({ ...form, title: "", description: "", terms: "", offer_code: "", destination_url: "", image_url: "", starts_at: today(), ends_at: "" }); setMessage("Offer submitted for SDTV approval."); } catch { } } return <main className="min-h-screen bg-slate-950 text-white"><MyHubHeader /><section className="mx-auto max-w-7xl px-6 py-10"><div className="flex flex-col gap-4 md:flex-row md:items-end md:justify-between"><div><p className="font-black uppercase tracking-wide text-pink-300">My Businesses</p><h1 className="mt-2 text-4xl font-black">Business Offers</h1><p className="mt-2 text-slate-300">Create offers for businesses you submitted or manage. Offers are reviewed before publication.</p></div><a href="/my-businesses" className="rounded-xl border border-white/20 px-5 py-3 text-center font-black">Back to My Businesses</a></div>{(error || message) && <div className="mt-6 rounded-2xl bg-white p-4 font-bold text-slate-950">{error || message}</div>}<div className="mt-8 grid gap-6 xl:grid-cols-[.9fr_1.1fr]"><section className="rounded-3xl bg-white p-6 text-slate-950"><h2 className="text-2xl font-black">Add an Offer</h2>{businesses.length === 0 ? <p className="mt-4 text-slate-500">No managed businesses are available. Submit or claim a business first.</p> : <div className="mt-5 grid gap-4"><label className="grid gap-1 font-bold">Business<select value={form.business_id} onChange={(e) => setForm({ ...form, business_id: e.target.value })} className="rounded-xl border p-3 font-normal"><option value="">Choose business...</option>{businesses.map((business) => <option key={business.id} value={business.id}>{business.name}</option>)}</select></label><label className="grid gap-1 font-bold">Offer title<input value={form.title} onChange={(e) => setForm({ ...form, title: e.target.value })} className="rounded-xl border p-3 font-normal"/></label><label className="grid gap-1 font-bold">Description<textarea value={form.description} onChange={(e) => setForm({ ...form, description: e.target.value })} className="min-h-28 rounded-xl border p-3 font-normal"/></label><div className="grid gap-4 sm:grid-cols-2"><label className="grid gap-1 font-bold">Starts<input type="date" value={form.starts_at} onChange={(e) => setForm({ ...form, starts_at: e.target.value })} className="rounded-xl border p-3 font-normal"/></label><label className="grid gap-1 font-bold">Ends<input type="date" value={form.ends_at} min={form.starts_at} onChange={(e) => setForm({ ...form, ends_at: e.target.value })} className="rounded-xl border p-3 font-normal"/></label></div><label className="grid gap-1 font-bold">Offer code<input value={form.offer_code} onChange={(e) => setForm({ ...form, offer_code: e.target.value })} className="rounded-xl border p-3 font-normal"/></label><label className="grid gap-1 font-bold">Offer link<input value={form.destination_url} onChange={(e) => setForm({ ...form, destination_url: e.target.value })} placeholder="https://..." className="rounded-xl border p-3 font-normal"/></label><label className="grid gap-1 font-bold">Image URL<input value={form.image_url} onChange={(e) => setForm({ ...form, image_url: e.target.value })} placeholder="Optional" className="rounded-xl border p-3 font-normal"/></label><label className="grid gap-1 font-bold">Terms<textarea value={form.terms} onChange={(e) => setForm({ ...form, terms: e.target.value })} className="min-h-20 rounded-xl border p-3 font-normal"/></label><button onClick={submit} disabled={saving} className="rounded-xl bg-pink-600 px-5 py-4 font-black text-white disabled:opacity-60">{saving ? "Submitting..." : "Submit Offer for Approval"}</button></div>}</section><section className="rounded-3xl bg-white p-6 text-slate-950"><h2 className="text-2xl font-black">Your Offers</h2>{loading ? <p className="mt-4">Loading...</p> : offers.length ? <div className="mt-5 grid gap-4">{offers.map((offer) => <article key={offer.id} className="rounded-2xl border p-5"><div className="flex items-start justify-between gap-3"><div><p className="text-xs font-black uppercase text-pink-600">{offer.local_businesses?.name}</p><h3 className="mt-1 text-xl font-black">{offer.title}</h3><p className="mt-2 text-sm text-slate-500">{offer.starts_at} – {offer.ends_at || "Ongoing"}</p></div><span className="rounded-full bg-slate-100 px-3 py-1 text-xs font-black capitalize">{offer.status}</span></div><div className="mt-3 flex gap-2 text-xs font-black"><span className="rounded-full bg-amber-50 px-3 py-1">Payment: {offer.payment_status}</span>{offer.is_premium && <span className="rounded-full bg-amber-200 px-3 py-1">Premium</span>}{offer.is_featured && <span className="rounded-full bg-pink-100 px-3 py-1">Featured</span>}</div>{["draft","pending","rejected"].includes(offer.status) && <button onClick={() => remove(offer.id)} className="mt-4 text-sm font-black text-red-600">Delete offer</button>}</article>)}</div> : <p className="mt-4 text-slate-500">No offers created yet.</p>}</section></div></section><SiteFooter /></main>; }
