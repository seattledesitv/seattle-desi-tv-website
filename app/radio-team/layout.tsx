import { staticMetadata } from "../lib/seo/service";
export const metadata = staticMetadata("Seattle Desi Radio Hosts and Team", "Meet the Seattle Desi Radio hosts and community voices presenting music, interviews, announcements, and local programming.", "/radio-team");
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
