"use client";

import { useEffect, useMemo, useState } from "react";
import MyHubHeader from "../components/MyHubHeader";
import SiteFooter from "../components/SiteFooter";
import { getSupabaseBrowserClient } from "../lib/supabaseBrowser";
import { resolveUserRole } from "../lib/roles";

const supabase = getSupabaseBrowserClient();
const cloudName = process.env.NEXT_PUBLIC_CLOUDINARY_CLOUD_NAME;
const uploadPreset = process.env.NEXT_PUBLIC_CLOUDINARY_UPLOAD_PRESET;
const interests = ["Events", "Culture", "Business", "Community News", "Photography", "Videography", "Writing", "Volunteering", "Influencer", "Sponsor", "Advertiser"];
type Tab = "profile" | "images" | "connected" | "social" | "interests" | "privacy";
type VisibilityMode = "private" | "credit" | "public";

function label(v?: string | null) { return String(v || "").replaceAll("_", " ") || "—"; }
function modeFrom(p: any): VisibilityMode { if (p?.keep_profile_private === false && (p?.show_name_publicly || p?.allow_social_credit)) return "public"; if (p?.show_name_publicly || p?.allow_social_credit) return "credit"; return "private"; }
function modeFlags(mode: VisibilityMode) { if (mode === "public") return { keep_profile_private: false, show_name_publicly: true, allow_social_credit: true }; if (mode === "credit") return { keep_profile_private: true, show_name_publicly: true, allow_social_credit: true }; return { keep_profile_private: true, show_name_publicly: false, allow_social_credit: false }; }
function modeTitle(mode: VisibilityMode) { return mode === "public" ? "Public Profile" : mode === "credit" ? "Credit OK" : "Private Profile"; }
function imageField(kind: string) { return kind === "team" || kind === "radio" ? "image" : kind === "volunteer" || kind === "influencer" ? "photo_url" : "profile_photo_url"; }
function imageUse(kind: string) { const m: any = { base: "Default SDTV account/profile photo. Use this everywhere to avoid uploading the same image again.", team: "Team profile image. Public Team page uses the official SDTV ID badge first when available, then this image.", volunteer: "Volunteer recognition and onboarding image.", radio: "Radio host/team image.", influencer: "Influencer directory image.", id: "Official SDTV ID badge uploaded by admins. The Team page uses this first when available." }; return m[kind] || "Connected profile image."; }
function Img({ src, title }: { src?: string; title: string }) { return <div className="rounded-2xl border bg-slate-50 p-3"><p className="text-xs font-black text-slate-500">{title}</p>{src ? <img src={src} alt={title} className="mt-2 h-36 w-full rounded-xl object-cover" /> : <div className="mt-2 grid h-36 place-items-center rounded-xl bg-white text-sm font-bold text-slate-400">No image yet</div>}</div>; }

export default function MyProfilePage() {
  const [loading, setLoading] = useState(true), [saving, setSaving] = useState(false), [uploading, setUploading] = useState(false);
  const [message, setMessage] = useState("Loading profile...");
  const [user, setUser] = useState<any>(null), [role, setRole] = useState("general_public");
  const [tab, setTab] = useState<Tab>("profile"), [mode, setModeState] = useState<VisibilityMode>("private");
  const [connected, setConnected] = useState<any[]>([]), [selected, setSelected] = useState<any>(null), [edit, setEdit] = useState<any>({});
  const [form, setForm] = useState<any>({ full_name: "", preferred_name: "", phone: "", email: "", city: "", state: "", country: "", short_bio: "", profile_photo_url: "", id_badge_url: "", instagram_url: "", facebook_url: "", linkedin_url: "", website_url: "", youtube_url: "", interests: [], allow_sdtv_contact: true });

  function setField(k: string, v: any) { setForm((c: any) => ({ ...c, [k]: v })); }
  function setEditField(k: string, v: any) { setEdit((c: any) => ({ ...c, [k]: v })); }
  function setVisibility(m: VisibilityMode) { setModeState(m); setForm((c: any) => ({ ...c, ...modeFlags(m) })); }
  function toggleInterest(x: string) { setForm((c: any) => ({ ...c, interests: c.interests?.includes(x) ? c.interests.filter((i: string) => i !== x) : [...(c.interests || []), x] })); }

  async function one(kind: string, title: string, query: any, statusField = "status") { const r = await query; if (r.error || !r.data) return null; const row = Array.isArray(r.data) ? r.data[0] : r.data; return row ? { kind, title, row, status: row[statusField] || row.role || row.title || "active" } : null; }
  async function loadConnected(u: any) {
    const email = u.email, id = u.id;
    const items = await Promise.all([
      one("base", "Base SDTV Profile", supabase.from("user_profiles").select("*").or(`user_id.eq.${id},email.eq.${email}`).limit(1).maybeSingle(), "role"),
      one("team", "Team Profile", supabase.from("team_members").select("*").or(`user_id.eq.${id},email.eq.${email}`).limit(1).maybeSingle(), "title"),
      one("radio", "Radio Profile", supabase.from("radio_team_members").select("*").or(`user_id.eq.${id},email.eq.${email}`).limit(1).maybeSingle(), "segment_name"),
      one("volunteer", "Volunteer Onboarding", supabase.from("volunteer_onboarding_submissions").select("*").or(`user_id.eq.${id},email.eq.${email}`).order("created_at", { ascending: false }).limit(1).maybeSingle(), "status"),
      one("influencer", "Influencer Profile", supabase.from("influencer_profiles").select("*").or(`user_id.eq.${id},email.eq.${email}`).limit(1).maybeSingle(), "status"),
      one("content", "Public Content Submissions", supabase.from("public_content_requests").select("*").or(`submitter_user_id.eq.${id},submitter_email.eq.${email}`).order("created_at", { ascending: false }).limit(1).maybeSingle(), "status"),
    ]);
    const clean = items.filter(Boolean);
    setConnected(clean);
    if (selected) { const newer = clean.find((i: any) => i.kind === selected.kind); if (newer) { setSelected(newer); setEdit(newer.row); } }
  }
  async function load() {
    setLoading(true); const { data } = await supabase.auth.getUser(); const u = data?.user || null; setUser(u);
    if (!u?.email) { setMessage("Please login to edit your SDTV profile."); setLoading(false); return; }
    const r = await resolveUserRole(supabase, u); setRole(r);
    const { data: p, error } = await supabase.from("user_profiles").select("*").eq("user_id", u.id).maybeSingle();
    if (error) { setMessage(`Could not load profile: ${error.message}`); setLoading(false); return; }
    const nextMode = modeFrom(p); setModeState(nextMode);
    const fallback = u.user_metadata?.full_name || u.user_metadata?.name || u.email.split("@")[0] || "";
    setForm((c: any) => ({ ...c, ...(p || {}), ...modeFlags(nextMode), full_name: p?.full_name || fallback, email: u.email, role: r, interests: p?.interests || [] }));
    await loadConnected(u); setMessage(p ? "" : "Set up your SDTV profile."); setLoading(false);
  }
  async function upload(file: File | undefined, target: "base" | "connected", field?: string) {
    if (!file) return; if (!file.type.startsWith("image/")) { setMessage("Please upload an image file."); return; }
    if (!cloudName || !uploadPreset) { setMessage("Image upload is not configured. Paste a URL instead."); return; }
    setUploading(true); const body = new FormData(); body.append("file", file); body.append("upload_preset", uploadPreset); body.append("folder", target === "base" ? "sdtv/user-profiles" : "sdtv/connected-profiles");
    try { const res = await fetch(`https://api.cloudinary.com/v1_1/${cloudName}/image/upload`, { method: "POST", body }); const out = await res.json(); if (!res.ok || !out.secure_url) throw new Error(out.error?.message || "Upload failed"); target === "base" ? setField("profile_photo_url", out.secure_url) : setEditField(field || "image", out.secure_url); setMessage("Image uploaded. Click Save to keep it."); } catch (e: any) { setMessage(e.message || "Upload failed"); } finally { setUploading(false); }
  }
  async function saveProfile() {
    if (!user?.id || !user?.email) return; setSaving(true); const flags = modeFlags(mode);
    const { error } = await supabase.from("user_profiles").upsert({ user_id: user.id, email: user.email, role, full_name: form.full_name || null, preferred_name: form.preferred_name || null, phone: form.phone || null, city: form.city || null, state: form.state || null, country: form.country || null, short_bio: form.short_bio || null, profile_photo_url: form.profile_photo_url || null, id_badge_url: form.id_badge_url || null, instagram_url: form.instagram_url || null, facebook_url: form.facebook_url || null, linkedin_url: form.linkedin_url || null, website_url: form.website_url || null, youtube_url: form.youtube_url || null, interests: form.interests || [], ...flags, allow_sdtv_contact: Boolean(form.allow_sdtv_contact), updated_at: new Date().toISOString() }, { onConflict: "user_id" });
    setMessage(error ? `Could not save profile: ${error.message}` : "Profile saved."); await loadConnected(user); setSaving(false);
  }
  async function saveConnected() {
    if (!selected) return; const map: any = { team: "team_members", radio: "radio_team_members", volunteer: "volunteer_onboarding_submissions", influencer: "influencer_profiles" }; const table = map[selected.kind];
    if (!table) { setMessage("This record is read-only here."); return; }
    const allowed: any = { team: ["name", "title", "image"], radio: ["name", "title", "segment_name", "image"], volunteer: ["full_name", "phone", "city", "photo_url"], influencer: ["full_name", "city", "bio", "photo_url", "instagram_url", "tiktok_url", "youtube_url", "website_url", "niche", "follower_count"] };
    const payload: any = { updated_at: new Date().toISOString() }; allowed[selected.kind].forEach((k: string) => { if (edit[k] !== undefined) payload[k] = edit[k] || null; });
    setSaving(true); const { error } = await supabase.from(table).update(payload).eq("id", selected.row.id); setMessage(error ? `Could not update ${selected.title}: ${error.message}` : `${selected.title} updated.`); await loadConnected(user); setSaving(false);
  }
  async function useBaseAll() {
    if (!form.profile_photo_url) { setMessage("Add a base profile photo first."); return; }
    const targets = connected.filter((i) => ["team", "radio", "volunteer", "influencer"].includes(i.kind)); setSaving(true);
    const results = await Promise.all(targets.map((i) => { const table: any = { team: "team_members", radio: "radio_team_members", volunteer: "volunteer_onboarding_submissions", influencer: "influencer_profiles" }; return supabase.from(table[i.kind]).update({ [imageField(i.kind)]: form.profile_photo_url, updated_at: new Date().toISOString() }).eq("id", i.row.id); }));
    const fail = results.find((r: any) => r.error); setMessage(fail ? `Some updates failed: ${fail.error.message}` : "Base profile photo applied to all connected profiles."); await loadConnected(user); setSaving(false);
  }
  function useBaseHere() { if (!form.profile_photo_url || !selected) return; setEditField(imageField(selected.kind), form.profile_photo_url); setMessage("Base profile photo copied here. Click Save to keep it."); }

  useEffect(() => { load(); }, []);
  const complete = useMemo(() => Math.round(([form.full_name, form.phone, form.city, form.short_bio, form.profile_photo_url, form.interests?.length ? "x" : ""].filter(Boolean).length / 6) * 100), [form]);
  const tabs: Tab[] = ["profile", "images", "connected", "social", "interests", "privacy"];

  return <main className="min-h-screen bg-slate-950 text-white"><MyHubHeader /><section className="mx-auto max-w-6xl px-6 py-10"><div className="mb-8"><p className="font-black uppercase tracking-wide text-pink-300">My SDTV</p><h1 className="mt-2 text-4xl font-black md:text-5xl">My Profile</h1><p className="mt-2 text-slate-300">Manage your SDTV identity, images, connected profiles, and visibility from one place.</p></div>{loading && <div className="rounded-3xl bg-white/10 p-6">{message}</div>}{!loading && !user?.id && <div className="rounded-3xl bg-white p-8 text-slate-950"><h2 className="text-2xl font-black">Login required</h2><a href="/login?next=/my-profile" className="mt-5 inline-flex rounded-xl bg-pink-600 px-5 py-3 font-black text-white">Login</a></div>}{!loading && user?.id && <div className="grid gap-5 lg:grid-cols-[340px_1fr]"><aside className="rounded-[2rem] bg-white p-6 text-slate-950 shadow-xl"><div className="grid place-items-center text-center"><div className="grid h-32 w-32 place-items-center overflow-hidden rounded-full bg-slate-100 text-4xl font-black text-pink-600">{form.profile_photo_url ? <img src={form.profile_photo_url} alt="Profile" className="h-full w-full object-cover" /> : (form.full_name || user.email || "S").slice(0,1).toUpperCase()}</div><h2 className="mt-4 text-2xl font-black">{form.full_name || "Your Profile"}</h2><p className="text-sm font-bold text-slate-500">{form.email}</p><span className="mt-3 rounded-full bg-pink-50 px-3 py-1 text-xs font-black text-pink-700">{modeTitle(mode)}</span></div><div className="mt-6 grid gap-3"><div className="rounded-2xl bg-slate-50 p-4"><p className="text-2xl font-black">{complete}%</p><p className="text-xs font-bold text-slate-500">Profile completeness</p></div><div className="rounded-2xl bg-slate-50 p-4"><p className="text-sm font-black">Current role</p><p className="mt-1 text-sm text-slate-600">{label(role)}</p></div><div className="rounded-2xl bg-slate-50 p-4"><p className="text-2xl font-black">{connected.length}</p><p className="text-xs font-bold text-slate-500">Connected profiles</p></div><label className="block text-sm font-bold">Upload Base Profile Photo<input type="file" accept="image/*" onChange={(e) => upload(e.target.files?.[0], "base")} className="mt-2 w-full rounded-xl border p-2 text-xs" /></label>{form.id_badge_url ? <div className="rounded-2xl bg-slate-50 p-3"><p className="text-xs font-black text-slate-500">SDTV ID Badge</p><img src={form.id_badge_url} alt="SDTV ID Badge" className="mt-2 rounded-xl border" /></div> : <div className="rounded-2xl bg-slate-50 p-3 text-xs text-slate-500"><b>No SDTV ID badge uploaded yet.</b><br />Once an admin uploads it, it will appear here.</div>}</div></aside><section className="rounded-[2rem] bg-white p-5 text-slate-950 shadow-xl md:p-8">{message && <div className="mb-5 rounded-2xl bg-yellow-50 p-4 font-bold text-yellow-900">{message}</div>}<div className="flex flex-wrap gap-2">{tabs.map((t) => <button key={t} onClick={() => setTab(t)} className={`rounded-xl px-4 py-2 text-sm font-black ${tab === t ? "bg-pink-600 text-white" : "bg-slate-100 text-slate-700"}`}>{t === "profile" ? "Profile" : t === "images" ? "Images & Usage" : t === "connected" ? "Connected Profiles" : t === "social" ? "Social" : t === "interests" ? "Interests" : "Privacy"}</button>)}</div>{tab === "profile" && <div className="mt-6 grid gap-4 md:grid-cols-2"><label className="font-bold">Full Name<input value={form.full_name || ""} onChange={(e) => setField("full_name", e.target.value)} className="mt-1 w-full rounded-xl border p-3 font-normal" /></label><label className="font-bold">Preferred Display Name<input value={form.preferred_name || ""} onChange={(e) => setField("preferred_name", e.target.value)} className="mt-1 w-full rounded-xl border p-3 font-normal" /></label><label className="font-bold">Email<input disabled value={form.email || ""} className="mt-1 w-full rounded-xl border bg-slate-100 p-3 font-normal text-slate-600" /></label><label className="font-bold">Phone<input value={form.phone || ""} onChange={(e) => setField("phone", e.target.value)} className="mt-1 w-full rounded-xl border p-3 font-normal" /></label><label className="font-bold">City<input value={form.city || ""} onChange={(e) => setField("city", e.target.value)} className="mt-1 w-full rounded-xl border p-3 font-normal" /></label><label className="font-bold">State<input value={form.state || ""} onChange={(e) => setField("state", e.target.value)} className="mt-1 w-full rounded-xl border p-3 font-normal" /></label><label className="font-bold md:col-span-2">Base Profile Photo URL<input value={form.profile_photo_url || ""} onChange={(e) => setField("profile_photo_url", e.target.value)} className="mt-1 w-full rounded-xl border p-3 font-normal" /></label><label className="font-bold md:col-span-2">Short Bio<textarea value={form.short_bio || ""} onChange={(e) => setField("short_bio", e.target.value)} className="mt-1 min-h-28 w-full rounded-xl border p-3 font-normal" /></label></div>}{tab === "images" && <div className="mt-6 space-y-4"><div className="rounded-2xl bg-pink-50 p-4 text-sm text-pink-900"><b>Image clarity:</b> the base photo can be reused everywhere. Team page will use SDTV ID badge first if available.</div><button onClick={useBaseAll} disabled={saving || !form.profile_photo_url} className="rounded-xl bg-slate-950 px-5 py-3 font-black text-white disabled:opacity-50">Use base profile photo for all connected profiles</button><div className="grid gap-4 md:grid-cols-2"><div className="rounded-3xl border p-4"><Img src={form.profile_photo_url} title="Base profile photo" /><p className="mt-3 text-sm text-slate-600">{imageUse("base")}</p></div><div className="rounded-3xl border p-4"><Img src={form.id_badge_url} title="SDTV ID badge" /><p className="mt-3 text-sm text-slate-600">{imageUse("id")}</p></div>{connected.filter((i) => ["team","radio","volunteer","influencer"].includes(i.kind)).map((i) => <div key={i.kind} className="rounded-3xl border p-4"><Img src={i.row?.[imageField(i.kind)]} title={`${i.title} image`} /><p className="mt-3 text-sm text-slate-600">{imageUse(i.kind)}</p></div>)}</div></div>}{tab === "connected" && <div className="mt-6"><p className="font-black">Your connected SDTV profiles</p><p className="mt-1 text-sm text-slate-500">Click a card to manage it here.</p><div className="mt-4 grid gap-3 md:grid-cols-2">{connected.map((i) => <button key={i.kind} onClick={() => { setSelected(i); setEdit(i.row); }} className={`rounded-2xl border p-4 text-left ${selected?.kind === i.kind ? "border-pink-500 bg-pink-50" : "bg-slate-50"}`}><h3 className="font-black">{i.title}</h3><p className="mt-1 text-sm text-slate-600">Status: {label(i.status)}</p></button>)}{connected.length === 0 && <div className="rounded-2xl bg-slate-50 p-5 text-slate-500">No connected profiles yet.</div>}</div>{selected && <div className="mt-5 rounded-3xl border bg-white p-5"><div className="flex items-start justify-between gap-3"><div><p className="text-xs font-black uppercase tracking-wide text-pink-600">Manage connected profile</p><h3 className="text-2xl font-black">{selected.title}</h3><p className="text-sm text-slate-500">Status: {label(selected.status)}</p></div><button onClick={() => setSelected(null)} className="rounded-xl bg-slate-100 px-3 py-2 text-xs font-black">Close</button></div>{["team","radio","volunteer","influencer"].includes(selected.kind) && <div className="mt-4 rounded-2xl bg-slate-50 p-4"><p className="font-black">Image used for this profile</p><p className="text-sm text-slate-600">{imageUse(selected.kind)}</p><div className="mt-3 grid gap-3 md:grid-cols-[160px_1fr]"><Img src={edit[imageField(selected.kind)]} title="Current image" /><div><button type="button" onClick={useBaseHere} disabled={!form.profile_photo_url} className="rounded-xl bg-pink-600 px-4 py-2 text-sm font-black text-white disabled:opacity-50">Use base profile photo here</button><label className="mt-3 block text-sm font-bold">Upload image for this profile<input type="file" accept="image/*" onChange={(e) => upload(e.target.files?.[0], "connected", imageField(selected.kind))} className="mt-2 w-full rounded-xl border p-2 text-xs" /></label><label className="mt-3 block text-sm font-bold">Image URL<input value={edit[imageField(selected.kind)] || ""} onChange={(e) => setEditField(imageField(selected.kind), e.target.value)} className="mt-1 w-full rounded-xl border p-3 font-normal" /></label></div></div></div>}<div className="mt-4 grid gap-3 md:grid-cols-2">{selected.kind === "team" && <><label className="font-bold">Name<input value={edit.name || ""} onChange={(e) => setEditField("name", e.target.value)} className="mt-1 w-full rounded-xl border p-3 font-normal" /></label><label className="font-bold">Title<input value={edit.title || ""} onChange={(e) => setEditField("title", e.target.value)} className="mt-1 w-full rounded-xl border p-3 font-normal" /></label></>}{selected.kind === "radio" && <><label className="font-bold">Name<input value={edit.name || ""} onChange={(e) => setEditField("name", e.target.value)} className="mt-1 w-full rounded-xl border p-3 font-normal" /></label><label className="font-bold">Title<input value={edit.title || ""} onChange={(e) => setEditField("title", e.target.value)} className="mt-1 w-full rounded-xl border p-3 font-normal" /></label><label className="font-bold md:col-span-2">Segment<input value={edit.segment_name || ""} onChange={(e) => setEditField("segment_name", e.target.value)} className="mt-1 w-full rounded-xl border p-3 font-normal" /></label></>}{selected.kind === "volunteer" && <><label className="font-bold">Full Name<input value={edit.full_name || ""} onChange={(e) => setEditField("full_name", e.target.value)} className="mt-1 w-full rounded-xl border p-3 font-normal" /></label><label className="font-bold">Phone<input value={edit.phone || ""} onChange={(e) => setEditField("phone", e.target.value)} className="mt-1 w-full rounded-xl border p-3 font-normal" /></label><label className="font-bold">City<input value={edit.city || ""} onChange={(e) => setEditField("city", e.target.value)} className="mt-1 w-full rounded-xl border p-3 font-normal" /></label><details className="rounded-2xl bg-slate-50 p-4 md:col-span-2"><summary className="cursor-pointer font-black">Onboarding acknowledgement</summary><p className="mt-3 text-sm"><b>Agreement acknowledged:</b> {edit.agreement_acknowledged ? "Yes" : "No"}</p><p className="text-sm"><b>Acknowledged at:</b> {edit.agreement_acknowledged_at ? new Date(edit.agreement_acknowledged_at).toLocaleString() : "—"}</p>{edit.agreement_text && <details className="mt-3 rounded-xl bg-white p-3"><summary className="cursor-pointer font-black">View exact agreement text accepted</summary><pre className="mt-3 whitespace-pre-wrap text-xs leading-5 text-slate-600">{edit.agreement_text}</pre></details>}</details></>}{selected.kind === "influencer" && <><label className="font-bold">Full Name<input value={edit.full_name || ""} onChange={(e) => setEditField("full_name", e.target.value)} className="mt-1 w-full rounded-xl border p-3 font-normal" /></label><label className="font-bold">City<input value={edit.city || ""} onChange={(e) => setEditField("city", e.target.value)} className="mt-1 w-full rounded-xl border p-3 font-normal" /></label><label className="font-bold">Niche<input value={edit.niche || ""} onChange={(e) => setEditField("niche", e.target.value)} className="mt-1 w-full rounded-xl border p-3 font-normal" /></label><label className="font-bold">Follower Count<input value={edit.follower_count || ""} onChange={(e) => setEditField("follower_count", e.target.value)} className="mt-1 w-full rounded-xl border p-3 font-normal" /></label><label className="font-bold md:col-span-2">Bio<textarea value={edit.bio || ""} onChange={(e) => setEditField("bio", e.target.value)} className="mt-1 min-h-24 w-full rounded-xl border p-3 font-normal" /></label><label className="font-bold">Instagram<input value={edit.instagram_url || ""} onChange={(e) => setEditField("instagram_url", e.target.value)} className="mt-1 w-full rounded-xl border p-3 font-normal" /></label><label className="font-bold">YouTube<input value={edit.youtube_url || ""} onChange={(e) => setEditField("youtube_url", e.target.value)} className="mt-1 w-full rounded-xl border p-3 font-normal" /></label></>}</div>{["team","radio","volunteer","influencer"].includes(selected.kind) && <button onClick={saveConnected} disabled={saving || uploading} className="mt-4 rounded-xl bg-slate-950 px-5 py-3 font-black text-white disabled:opacity-60">{saving ? "Saving..." : `Save ${selected.title}`}</button>}</div>}</div>}{tab === "social" && <div className="mt-6 grid gap-4 md:grid-cols-2"><input value={form.instagram_url || ""} onChange={(e) => setField("instagram_url", e.target.value)} placeholder="Instagram URL" className="rounded-xl border p-3" /><input value={form.linkedin_url || ""} onChange={(e) => setField("linkedin_url", e.target.value)} placeholder="LinkedIn URL" className="rounded-xl border p-3" /><input value={form.facebook_url || ""} onChange={(e) => setField("facebook_url", e.target.value)} placeholder="Facebook URL" className="rounded-xl border p-3" /><input value={form.website_url || ""} onChange={(e) => setField("website_url", e.target.value)} placeholder="Website URL" className="rounded-xl border p-3" /><input value={form.youtube_url || ""} onChange={(e) => setField("youtube_url", e.target.value)} placeholder="YouTube URL" className="rounded-xl border p-3 md:col-span-2" /></div>}{tab === "interests" && <div className="mt-6"><p className="font-black">What are you interested in?</p><div className="mt-4 flex flex-wrap gap-2">{interests.map((x) => <button key={x} type="button" onClick={() => toggleInterest(x)} className={`rounded-xl px-3 py-2 text-sm font-black ${form.interests?.includes(x) ? "bg-pink-600 text-white" : "bg-slate-100 text-slate-700"}`}>{x}</button>)}</div></div>}{tab === "privacy" && <div className="mt-6 space-y-4"><p className="font-black">Public Visibility</p><div className="grid gap-3">{(["private", "credit", "public"] as VisibilityMode[]).map((m) => <button key={m} type="button" onClick={() => setVisibility(m)} className={`rounded-2xl border p-4 text-left ${mode === m ? "border-pink-500 bg-pink-50" : "border-slate-200 bg-white"}`}><p className="font-black">{modeTitle(m)}</p><p className="text-sm text-slate-600">{m === "public" ? "SDTV may show my profile publicly where appropriate." : m === "credit" ? "Keep my profile private, but SDTV may credit my name when publishing my content." : "Used internally only by default."}</p></button>)}</div><label className="block rounded-2xl bg-slate-50 p-4"><input type="checkbox" checked={Boolean(form.allow_sdtv_contact)} onChange={(e) => setField("allow_sdtv_contact", e.target.checked)} /> <b>Allow SDTV to contact me</b></label></div>}<button onClick={saveProfile} disabled={saving || uploading} className="mt-8 rounded-xl bg-pink-600 px-6 py-4 font-black text-white disabled:opacity-60">{uploading ? "Uploading..." : saving ? "Saving..." : "Save Profile"}</button></section></div>}</section><SiteFooter /></main>;
}
